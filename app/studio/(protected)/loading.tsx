import AnimatedFairy from "../../../components/animated-fairy";

export default function StudioLoading() {
  return <div aria-label="Loading Studio workspace" className="studio-workspace-loading" role="status"><AnimatedFairy size={116} intensity={0.82} /></div>;
}
